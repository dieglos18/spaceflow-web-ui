# spaceflow-web-ui
Frontend application for coworking reservation system

## Design system
- **Icons**: Use `icon-blue.svg` on light backgrounds (e.g. landing card). Use `icon-white.svg` on dark or primary (space-blue) backgrounds (e.g. header, footer, CTAs).
